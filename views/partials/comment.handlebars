
<div class="comments">
    {{#each this}}
        <div class="row">
            <div class="col s12">
                <div class="card">
                    <div class="card-content">
                        <p>
                            {{comment}}
                        </p>
                            <p class="text-end"> -{{user.name}}, {{format_date date_created}}</p>
                    </div>
                </div>
            </div>
        </div>
    {{/each}}
</div>